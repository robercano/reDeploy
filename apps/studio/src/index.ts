export const name = "@redeploy/studio";

/**
 * Placeholder export so the package builds and tests green before the
 * visual authoring/inspection UI (React + React Flow) lands.
 */
export function placeholder(): string {
  return name;
}
